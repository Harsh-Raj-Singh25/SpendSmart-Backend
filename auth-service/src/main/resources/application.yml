server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/spendsmart_auth
    username: root
    password: Harsh@SQL12
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000

# Google OAuth2 Client ID — replace with your actual Google Console client ID.
# To get one: https://console.cloud.google.com → APIs & Services → Credentials
# Create an "OAuth 2.0 Client ID" of type "Web application"
google:
  client-id: PLACEHOLDER_GOOGLE_CLIENT_ID

eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka/
