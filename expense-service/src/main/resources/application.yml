server:
  port: 8082

spring:
  application:
    name: expense-service
  datasource:
    url: jdbc:mysql://localhost:3306/spendsmart_expense?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: Harsh@SQL12 # Update this!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true