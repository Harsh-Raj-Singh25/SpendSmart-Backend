server:
  port: 8088

spring:
  application:
    name: notification-service
  datasource:
    url: jdbc:mysql://localhost:3306/spendsmart_notification?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: Harsh@SQL12
  
  # ADD THIS BLOCK RIGHT HERE
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: harshrajsingh252@gmail.com
    password: etramjjvkhibkioy # app password 
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true