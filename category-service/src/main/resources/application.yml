server:
  port: 8084

spring:
  application:
    name: category-service
  datasource:
    # ADDED allowPublicKeyRetrieval=true TO THE END OF THIS LINE
    url: jdbc:mysql://localhost:3306/spendsmart_category?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: Harsh@SQL12  
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
     service-url:
        defaultZone: http://localhost:8761/eureka/